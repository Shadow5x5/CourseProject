import classes from "./LogoText.module.css";

const LogoText = () => {
    return (
        <div>
            <h1 className={classes.logo}>IHS WINE</h1>
        </div>
    );
};

export default LogoText;
